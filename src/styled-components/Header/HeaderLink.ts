import styled from '../styled-components';

const HeaderLink = styled.h5`
  color: ${props => props.theme.offWhite};
  padding: 0 2rem 0 0;
  font-weight: lighter;
  cursor: pointer;
`;

export default HeaderLink;
